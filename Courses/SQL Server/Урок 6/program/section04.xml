<?xml version="1.0" encoding="windows-1251" ?>

<!DOCTYPE page [
<!ENTITY nbsp "&#160;">
<!ENTITY пробел "&#160;">
<!ENTITY shy "&#173;">
<!ENTITY перенос "&#173;">
]>

<?xml:stylesheet type="text/xsl" href="../common/layout.xsl" ?>

<Урок xmlns="x-schema:Schema.xml" название="Системные таблицы." 
предыдущий="section03.xml" следующий="section05.xml" copyright="© 2005 Вадим Волянский, Алексей Туманов.">
<h1 style="color:maroon">Системные таблицы.</h1>

<p>
При создании базы данных вы сразу же получаете набор системных таблиц. Вы их, конечно же, видели. Их имена начинаются с sys. Вот и пришло время узнать, какую информацию они хранят и для чего они служат.
</p>

<p>
Повторюсь. Любая создаваемая база данных представляет из себя копию базы данных model. Т.о. все системные таблицы, в исходном виде, можно найти в ней.
</p>

<h3 align='center'><font color="#1925AB">syscolumns</font></h3>

<p>
В ней содержится общее описание по каждому из полей таблиц, представлений и по каждому из параметров хранимых процедур, которые есть в базе данных. Например, обязательное ли поле для заполнения, либо нет, установлено ли значение по умолчанию и пр.
</p>

<h3 align='center'><font color="#1925AB">syscomments</font></h3>

<p>
Эта таблица хранит синтаксис определения все представлений, ограничений, хранимых процедур и триггеров. При удалении значения из этой таблицы вручную, может привести к некорректной работе того объекта, определение которого было удалено.
</p>

<h3 align='center'><font color="#1925AB">sysdepends</font></h3>

<p>
В этой таблице сохраняется информация о зависимостях между представлениями, хранимыми процедурами, триггерами и таблицами, которые указываются при их определении.
</p>

<h3 align='center'><font color="#1925AB">sysfilegroups</font></h3>

<p>
Содержит информацию о Группах файлов, которые есть в базе данных.
</p>

<h3 align='center'><font color="#1925AB">sysfiles</font></h3>

<p>
В ней хранится информация о логическом и физическом названиях файлов, о их максимальном размере и пр. Эта таблица является виртуальной и не может быть модифицирована напрямую.
</p>

<h3 align='center'><font color="#1925AB">sysаforeignkeys</font></h3>

<p>
Хранит информацию обо всех ограничениях типа Внешний Ключ. Т.е. указывается внешний ключ какой таблицы находится в связи с первичным ключом другой таблицы.
</p>

<h3 align='center'><font color="#1925AB">sysindexes</font></h3>

<p>
Хранит информацию обо всех индексах, которые есть в базе данных.
</p>

<h3 align='center'><font color="#1925AB">sysindexkeys</font></h3>

<p>
Содержит информацию о индексах и полях, на которые они указывают.
</p>

<h3 align='center'><font color="#1925AB">sysmembers</font></h3>

<p>
В этой таблице храниться информация о пользователях и ролях, к которым они относятся. Роль - это набор прав и ограничений, который определяет возможность запрета доступа к одним данным и разрешение на доступ, и уровень доступа к другим данным.
</p>

<h3 align='center'><font color="#1925AB">sysobjects</font></h3>

<p>
Если таблица syscomments хранит синтаксис ограничений и хранимых процедур, то в этой таблице находится их описание. Например, уникальный номер, идентификатор владельца и т.д.
</p>

<h3 align='center'><font color="#1925AB">syspermissions</font></h3>

<p>
Здесь указываются права и ограничения, установленные на определенного пользователя, группу или роль.
</p>

<h3 align='center'><font color="#1925AB">sysreferences</font></h3>

<p>
Содержит карту связей таблиц.
</p>

<h3 align='center'><font color="#1925AB">systypes</font></h3>

<p>
Хранит список все типов данных (пользовательских и системных), которые могут использоваться при создании таблиц.
</p>

<h3 align='center'><font color="#1925AB">sysusers</font></h3>

<p>
В этой таблице указываются все пользователи и роли SQL Server и Windows.
</p>


</Урок>