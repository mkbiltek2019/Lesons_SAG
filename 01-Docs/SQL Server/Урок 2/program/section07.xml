<?xml version="1.0" encoding="windows-1251" ?>

<!DOCTYPE page [
<!ENTITY nbsp "&#160;">
<!ENTITY пробел "&#160;">
<!ENTITY shy "&#173;">
<!ENTITY перенос "&#173;">
]>

<?xml:stylesheet type="text/xsl" href="../common/layout.xsl" ?>

<Урок xmlns="x-schema:Schema.xml" название="Оператор INSERT"  предыдущий="section04.xml" следующий="section08.xml" copyright="© 2005 Вадим Волянский, Алексей Туманов.">

<h2><font color="#800000">Оператор INSERT.</font></h2>

<p>
Оператор Insert служит для добавления данных в таблицу. Существует два варианта синтаксиса запроса на добавление информации.
</p>

<ol>
	<li>
	<pre  style="border:3px #FF0000 solid">
	Insert into имя_таблицы<br/>
	values (значение1, значение2, значение3... значениеN)
	</pre>
	<p>
	В этом случае кол-во перечисленных значений должно строго соответствовать порядку и кол-ву полей в указанной таблице.
	</p>
	</li>
	<li>
	<pre  style="border:3px #FF0000 solid">
	Insert into имя_таблицы (имя_поля1, имя_поля2, имя_поля3)<br/>
	values (значение1, значение2, значение3)
	</pre>
	<p>
	В этом случае, кол-во значений должно строго совпадать кол-ву перечисленных столбцов.
	</p>
	</li>
</ol>

<p>
Пример на первый вариант.
</p>

<pre>
Insert into books<br/>
Values(769, 4895 , 1, 'Справочник по С 4-ое издание', 68.93, 'Питер',<br/>
685, '180*240/30', 2005-08-29, 5000, 'Программирование', 'Справочник')
</pre>

<p>
Вы должны помнить. что любая строка, указываемая в запросе должна быть заключена в ОДИНАРНЫЕ КАВЫЧКИ. Числа, указываемые в запросе, в кавычки НЕ ЗАКЛЮЧАЮТСЯ!!!
</p>

<p>
При таком синтаксисе, запрос на добавление можно вызвать не более одного раза, т.к. в любой таблице должно существовать поле, являющиеся первичным ключом, значения в котором повторяться не могут. В примере запроса туда записывается значение 769. При попытке второй раз сохранить значение 769 в поле, являющиеся первичным ключом приведет к ошибке. (Учитывая, что база данных books импортировалась, первичный ключ в таблице books отсутствует.)
</p>

<p>
Иногда требуется заполнить только несколько столбцов из всей таблицы. Для этого используется второй вариант синтаксиса запроса на вставку. Но здесь возможна ситуация, когда некоторые поля в таблице будут являться обязательными для заполнения. В этом случае необходимо их также перечислять при заполнении.
</p>

<pre>
Insert into books(N,New,Name,Date,Izd)<br/>
Values(770,1,'Microsoft SQL Server 2000',2005-08-29,'Бином')
</pre>

</Урок>